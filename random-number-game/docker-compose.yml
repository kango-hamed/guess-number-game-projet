version: '3.8'

services:
  # Serveur GuessCraft
  guesscraft-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: guesscraft-server
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - guesscraft-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  guesscraft-network:
    driver: bridge
